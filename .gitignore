node_modules/
dist/
.env
.env.local
*.local
# GeoJSON files are re-downloaded at CI build time — don't commit stale data
public/data/*.geojson
# KML exports are committed so users can download them directly from the repo

